#ifndef ARVORE_H
#define ARVORE_H

#include "lista.h"
#include "fila.h"


/*Aqui a arvore é uma abstração para a busca pelo resultado, que é basicamente uma LIFO recursivo*/
class Arvore
{


private:

public:

	static Fila* fila;
	static Lista * referencia;

	static int inica_busca(ml);

	static int busca(const ml);
	
	
};

#endif